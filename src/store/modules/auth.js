//store/modules/auth.js

import axios from 'axios';
import store from '..';

const state = {
    user: null,
    posts: null
}

const getters = {
    
}

const actions = {
    async register() {
        await store.commit
    }
}

const mutations = {
    
}

export default {
    state,
    getters,
    actions,
    mutations
}